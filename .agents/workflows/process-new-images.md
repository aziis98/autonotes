---
description: Check if up to date
---

Run `go run -v . status` and check for new images to process. Only create new .note files and at the end of the conversation suggest if the user wants to also have create a new cards/\*.md file.
